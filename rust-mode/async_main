# -*- mode: snippet -*-
# name: async fn main() { ... }
# key: async_main
# --
#[tokio::main]
async fn main() -> color_eyre::eyre::Result<()> {
	color_eyre::install()?;
	tracing_subscriber::fmt::init();

	let args = Args::parse();
	tracing::trace!("Args: {args:?}");

	$0

	Ok(())
}